# Upgrade core build tools first
pip>=25.3
setuptools>=75.0.0
wheel>=0.44.0

# Core libraries for face recognition
face-recognition==1.3.0
face-recognition-models==0.3.0
dlib==19.24.2

# Image handling
Pillow==10.1.0

# Numerical and scientific tools
numpy>=1.26.0
opencv-python==4.10.0.84

# Optional but common utilities
click==8.1.7
Flask==3.0.3
gunicorn==23.0.0

# Use only prebuilt binaries to avoid compiling in Render
--only-binary=:all:
