This directory holds project static assets (CSS, JS, images) that are collected by `collectstatic` into `STATIC_ROOT`.

Add your files under appropriate subdirectories, for example:

- css/
- js/
- images/

This empty README exists to document the folder and ensure the directory is tracked by Git.
